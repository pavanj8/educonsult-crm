# MiniMax is OpenAI-compatible; the harness engine (minimax_agent.py) drives
# the agents through this client (docs/adr/0019).
openai>=1.30

# NOTE: cursor-sdk is intentionally NOT installed. The egress probe
# (docs/adr/0019 follow-up) showed that merely having cursor-sdk on the path
# reroutes the `openai` client onto the Cursor gateway (identical openai
# version, destination flips), which is exactly why MiniMax model IDs were
# rejected with Cursor's catalog. The CURSOR_API_KEY env var can stay set
# (dormant) for a possible rollback, but the package must be absent.
